name: Push directory to another repository
on: 
  pull_request:
    branches:
      - "master"
jobs:
  deploy-user-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Get index.md
        id: index
        run: echo "CI"
